package  ${daoPackage};
import ${pojoPackage}.${myClass.className};
import java.util.ArrayList;
import java.util.List;
import org.springframework.stereotype.Repository;
import ${daoPackage}.base.${baseDaoImpl};
@Repository
public class ${daoImpl} extends ${baseDaoImpl}<${myClass.className}, ${myClass.idField.type}>implements ${daoI}{
	
}
