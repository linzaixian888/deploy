package ${servicePackage};

import ${pojoPackage}.${myClass.className};
import ${servicePackage}.base.${baseServiceI};

public interface ${serviceI} extends ${baseServiceI}<${myClass.className},${myClass.idField.type}>{

}
